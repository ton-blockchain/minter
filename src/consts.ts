const ROUTES = {
    connect: '/connect',
    deployer: '/',
}

const APP_GRID = 1000

const LOCAL_STORAGE_PROVIDER = 'wallet_provider'

const APP_NAME = 'deployer'

const JETTON_DEPLOYER_CONTRACTS_GITHUB = 'https://github.com/ton-defi-org/jetton-deployer-contracts'

export {ROUTES, APP_NAME, LOCAL_STORAGE_PROVIDER, APP_GRID, JETTON_DEPLOYER_CONTRACTS_GITHUB}